apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  annotations:
    apps.open-cluster-management.io/reconcile-rate: high
  name: umbrella-gitops
  namespace: umbrella-gitops
spec:
  type: GitHub
  pathname: https://github.com/nasx/phoenix.git
